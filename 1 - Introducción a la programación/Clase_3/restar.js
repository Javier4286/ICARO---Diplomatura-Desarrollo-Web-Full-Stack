/* 2- Crear un archivo restar.js que contenga una función llamada restar, la cual debe ser exportada al final del archivo.
Esta función deberá recibir 2 parámetros y retornar la resta de los mismos. */

function restar(numero1, numero2) {
  return numero1 - numero2;
}
module.exports = { restar };
